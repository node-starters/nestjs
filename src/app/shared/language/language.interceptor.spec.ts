import { LanguageInterceptor } from './language.interceptor';

describe('LanguageInterceptor', () => {
  it('should be defined', () => {
    expect(new LanguageInterceptor()).toBeDefined();
  });
});
